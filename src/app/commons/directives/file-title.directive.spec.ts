import { FileTitleDirective } from './file-title.directive';

describe('FileTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FileTitleDirective();
    expect(directive).toBeTruthy();
  });
});
